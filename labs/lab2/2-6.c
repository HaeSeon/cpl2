#include <stdio.h>
#include <limits.h>

int main(void){

	printf("int min is %d, int max is %d\n", INT_MIN, INT_MAX);
	printf("int min-1 is %d, int max+1 is %d\n", INT_MIN-1, INT_MAX+1);

	return 0;
}
